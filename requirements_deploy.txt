# IMPORTANT: install torch/torchvision FIRST, from the CPU-only wheel index
# (see the Dockerfile), before this file:
#   pip install torch==2.2.0+cpu torchvision==0.17.0+cpu \
#       --index-url https://download.pytorch.org/whl/cpu
# `timm` below depends on torch/torchvision transitively; skipping the step
# above pulls the default multi-GB CUDA build instead.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
redis>=5.0.0
numpy>=1.24.0
scipy>=1.11.0
Pillow>=10.0.0
pydantic>=2.0.0
timm>=0.9.0
